import {createStyleSheet} from 'react-native-unistyles';

export const stylesheet = createStyleSheet(() => ({
  button: {
    position: 'absolute',
  },
  container: {
    backgroundColor: 'red',
  },
}));
